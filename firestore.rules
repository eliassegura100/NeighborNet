rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {

    function isSignedIn() { return request.auth != null; }
    // If you keep these, use them in allow statements
    // function isRequester() { return isSignedIn() && request.auth.token.role == 'requester'; }
    // function isVolunteer() { return isSignedIn() && request.auth.token.role == 'volunteer'; }

    match /users/{uid} {
      allow read: if isSignedIn();
      allow write: if isSignedIn() && request.auth.uid == uid;
    }

    match /requests/{id} {
      allow read: if true;
      allow write: if false; // server (Cloud Functions) only
    }

    match /impactMetrics/{doc} {
      allow read: if true;
      allow write: if false; // server only
    }
  }
}